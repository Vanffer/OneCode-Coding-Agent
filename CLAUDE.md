# OneCode Coding Agent

一个用 Go 语言构建的编码代理（Coding Agent）项目。One双关，既表示一个的意思，又表示小狗汪汪汪的叫声（所以logo是一条小狗）

## 项目目标

为用户提供智能编码辅助，能够理解需求、规划实现方案、编写和验证代码。

## 开发流程

本项目使用 Spec 驱动开发流程。在开始任何功能开发前，运行 `/mew-spec` 启动流程：

```
spec.md → plan.md → task.md → checklist.md → 开发 → 验收
```

四份文档全部通过审批后才能编写实现代码。

## 测试

开发完功能后，另启终端做端到端测试：

1. 在 终端中启动 OneCode
2. 输入一段真实的对话请求
3. 观察 OneCode 是否正确调用工具、生成回复
4. 对照 checklist.md 逐项验收

## 技术栈

- **语言：** Go
- **项目状态：** 初始化阶段
